<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="stylesheets/main.css">
    <title>Route Buddy</title>
    
  </head>
  
  <body>
    <header id="content" >
      <nav class="main-navigation-bar">
        <ul>
          <li>
            <a href="/"><button class="nav-button" type="button">Home</button></a>
          </li><li>
            <a href="/view_entries"><button class="nav-button" type="button">View Entries</button></a>
          </li><li>
            <a href="/add_entry"><button class="nav-button" type="button">Add New Entry</button></a>
          </li>
        </ul>
      </nav>
    </header>
     
     <% if session[:error] %>
      <div class="flash error">
        <p><%= session.delete(:error) %></p>
      </div>
    <% end %>

    <% if session[:success] %>
      <div class="flash success">
        <p><%= session.delete(:success) %></p>
      </div>
    <% end %>

    <section class="main-content">
      <%= yield %>
    </section>
    
    <section class="search-bar">
      <h2 class="content-subhead">Search Entries</h2>
        <form action="/search" method="get">
          <input name="query" value="<%= params[:query] %>">
          <button type="submit">Search</button>
        </form>
    </section>
  
  <footer>
    <small>Route Buddy</small>
  </footer>
  </body>
</html>